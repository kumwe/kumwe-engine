name: Native source release
on:
  pull_request:
    paths:
      - '.github/workflows/release.yml'
      - 'tools/release-native.py'
      - 'tools/release-native-test.py'
      - 'tools/release-validation/**'
  workflow_run:
    workflows: ['Native quality']
    types: [completed]
  workflow_dispatch:
    inputs:
      quality_run_id:
        description: Successful Native quality run for this exact default-branch commit
        type: string
        required: true
      candidate_attestation_uri:
        description: Immutable SDK raw YAML URI, or omit to read the exact merged PR reference
        type: string
        required: false
      candidate_attestation_sha256:
        description: SHA256 of the external candidate YAML bytes
        type: string
        required: false
permissions:
  contents: read
concurrency:
  group: native-source-release-${{ github.repository }}
  cancel-in-progress: false
jobs:
  publisher-tests:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          persist-credentials: false
      - name: Verify source publication identity, provenance and immutable retry guards
        run: python3 tools/release-native-test.py
      - name: Install exact candidate schema validator dependencies
        working-directory: tools/release-validation
        run: npm ci --ignore-scripts --no-audit
      - name: Verify full candidate schema and cross-build refusal cases
        run: node --test tools/release-validation/candidate-gate.test.mjs
  publish:
    needs: publisher-tests
    if: >-
      github.ref == format('refs/heads/{0}', github.event.repository.default_branch) &&
      (github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'workflow_run' &&
      github.event.workflow_run.head_repository.full_name == github.repository &&
      github.event.workflow_run.head_branch == github.event.repository.default_branch &&
      github.event.workflow_run.event == 'push' &&
      github.event.workflow_run.head_sha == github.sha &&
      github.event.workflow_run.conclusion == 'success'))
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    permissions:
      contents: write
      actions: read
      id-token: write
      attestations: write
    env:
      GH_TOKEN: ${{ github.token }}
      DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
      QUALITY_RUN_ID: ${{ github.event.workflow_run.id || inputs.quality_run_id }}
      ENGINE_CANDIDATE_ATTESTATION_URI: ${{ inputs.candidate_attestation_uri }}
      ENGINE_CANDIDATE_ATTESTATION_SHA256: ${{ inputs.candidate_attestation_sha256 }}
    steps:
      - name: Check out the exact trusted workflow source with complete tag history
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
          persist-credentials: false
      - name: Install exact candidate schema validator dependencies before publication verification
        working-directory: tools/release-validation
        run: npm ci --ignore-scripts --no-audit
      - name: Require exact full CI and all stable source prerequisites
        id: source
        run: python3 tools/release-native.py prepare "$RUNNER_TEMP/native-source-bundle"
      - name: Sign the five exact source evidence files with GitHub OIDC
        if: steps.source.outputs.already_published != 'true'
        id: provenance
        uses: actions/attest@1e69f48acb82d1966a394da916b4c1698aa569d6 # v4.2.2
        with:
          subject-path: ${{ runner.temp }}/native-source-bundle/*
      - name: Verify signed source identity and publish missing immutable assets
        if: steps.source.outputs.already_published != 'true'
        env:
          PROVENANCE_PATH: ${{ steps.provenance.outputs.bundle-path }}
        run: >-
          python3 tools/release-native.py publish "$RUNNER_TEMP/native-source-bundle"
          --provenance "$PROVENANCE_PATH"
